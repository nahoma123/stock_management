# .env

# PostgreSQL Connection
POSTGRES_USER=socialuser          # Choose a username
POSTGRES_PASSWORD=your_strong_password # Choose a strong password
POSTGRES_DB=socialgraphicpro    # Choose a database name
DB_HOST=db                       # Docker service name for the database
DB_PORT=5432                     # Standard PostgreSQL port

# Full Database URL for Drizzle/Node-postgres
# Format: postgresql://USER:PASSWORD@HOST:PORT/DB
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${DB_HOST}:${DB_PORT}/${POSTGRES_DB}

# Application Ports (Optional, but good practice)
BACKEND_PORT=3000                # Port the backend server will listen on inside the container
FRONTEND_PORT=5173               # Port the Vite dev server will listen on inside the container

# Client-side variable pointing to the backend API
# This needs to be prefixed based on your frontend framework (e.g., VITE_)
# Check how your client code reads the API URL. Assuming Vite:
VITE_API_URL=http://localhost:3000 # URL frontend uses to talk to backend (accessible from your browser)

# Add any other environment variables your application might need (API keys, secrets, etc.)
# e.g., JWT_SECRET=your_jwt_secret